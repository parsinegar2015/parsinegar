(SELECT t.title as 'thesis_title', concat_ws(' ',u.fname,u.lname) as 'thesis_fullname', t.date as 'thesis_date', null as 'article_title',null as 'article_title_en', null as 'article_fullname', null as 'article_date', null as 'article_file' from `thesis` as t 
inner join user as u 
on t.uId = u.id
WHERE t.title like '%:param1%' OR concat_ws(' ',u.fname,u.lname) like '%:param2%' OR t.keyword like '%:param3%' AND t.status = 1 AND YEAR(t.date)=':param9' 
ORDER BY t.id
DESC LIMIT 0, 10) 
UNION
(SELECT null,null,null,a.title,a.title_en,concat_ws(' ',u.fname,u.lname),a.date,a.file from `article` as a
inner join user as u 
on a.uId = u.id 
inner join article_field as af
on a.id = af.aId
WHERE af.fId in(1,2,3) and a.title like '%:param4%' OR concat_ws(' ',u.fname,u.lname) like '%:param5%' OR a.keyword like '%:param6%' OR a.keyword_en like '%:param7%' OR a.title_en like '%:param8%' AND a.status = 1 AND YEAR(a.date)=':param10'
ORDER BY a.id 
DESC LIMIT 0, 10)